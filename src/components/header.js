import React, { useState, useEffect } from 'react';

const Header = () => {
  const [,] = useState();

  useEffect(() => {}, []);

  return (
    <div className="App-header">
      <div>Memory Game</div>
    </div>
  );
};

export default Header;
